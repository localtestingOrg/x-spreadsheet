framework: mocha
preRun:
     command:
        - npm install
        - npm run build
preMerge:
  pattern:
    - "./test/core/*.js"
postMerge:
  pattern: 
    - "./test/core/*.js"
postRun:
     command:
        - node --version
configFile: "mocharc.yml"
version: 1.0